server:
  port: 0  # Use random port for tests

logging:
  level:
    com.example: DEBUG
    org.springframework.web: DEBUG

spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: none